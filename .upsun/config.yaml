# Upsun configuration, generated using AI at: 2026-02-11T22:18:44-05:00
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  app:
    type: python:3.13

    hooks:
      build: |
        set -ex
        pip install -r requirements.txt

    web:
      commands:
        start: python server.py

    mounts:
      app_data:
        source: storage

routes:
  https://{default}/:
    type: upstream
    upstream: app:http